# Install

Install Rust using the toolchain pinned in `rust-toolchain.toml`.

Run the local validation suite:

```bash
make ci
```

Install local hooks:

```bash
make install
```

Useful commands:

```bash
make fmt-check
make check
make test
make docs-check
make docs-build
make scripts-check
```
